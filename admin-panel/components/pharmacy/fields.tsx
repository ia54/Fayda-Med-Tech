import {Input} from '@/components/ui/input'
export function Field({name,label,type='text',required=true,...props}: {name:string;label:string;type?:string;required?:boolean;defaultValue?:string|number;min?:string|number;max?:string|number;step?:string|number}) {return <label className="grid gap-1 text-sm"><span>{label}</span><Input name={name} type={type} required={required} {...props}/></label>}
export function Choice({name,label,options,defaultValue}: {name:string;label:string;options:string[];defaultValue?:string}) {return <label className="grid gap-1 text-sm"><span>{label}</span><select name={name} defaultValue={defaultValue} className="h-10 border rounded-md px-3 bg-background">{options.map(v=><option key={v} value={v}>{v.replaceAll('_',' ')}</option>)}</select></label>}
export function errorMessage(e:unknown):string {const x=e as {data?:{message?:string;errors?:Record<string,string[]>}};return Object.values(x.data?.errors||{}).flat().join(' ')||x.data?.message||'Could not save. Check your connection and try again.'}
